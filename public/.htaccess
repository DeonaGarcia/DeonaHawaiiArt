RewriteEngine On
RewriteBase /

# Allow direct access to static files (CSS, JS, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Allow direct access to index.html and index.php
RewriteRule ^index\.(html|php)$ - [L]

# SPA fallback - route everything else to index.php for PHP hosting compatibility
RewriteRule . /index.php [L]
